#pragma once
#include "Library.h"
#include "Database_manager.h"

class ui_db_context
{
	//std::function <bool(const typename Database_manager::database_entry, const typename Database_manager::database_entry) > sorting = std::bind ;
	//std::function<bool const typename database_manager::database_entry>;
};

